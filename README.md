# Cs-4120-Project
Project proposal using the UCI Cleveland Heart Disease dataset. Binary classification (disease present vs not) and regression (cholesterol, max heart rate). Includes baselines, metrics (Acc, F1, ROC-AUC, MAE, RMSE), reproducibility with pinned deps &amp; MLflow.
